<Window x:Class="Ecad.App.Views.BomGroupingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bill of Materials" Height="240" Width="320"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Group by:" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,8"/>
        <RadioButton x:Name="ProjectRadio" Content="Project (flat total)" Grid.Row="1" Margin="0,0,0,4" IsChecked="True"/>
        <RadioButton x:Name="LocationRadio" Content="Location" Grid.Row="2" Margin="0,0,0,4"/>
        <RadioButton x:Name="CableAssemblyRadio" Content="Cable assembly" Grid.Row="3" Margin="0,0,0,4"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="5" Margin="0,8,0,0">
            <Button Content="OK" Width="80" Margin="0,0,8,0" Click="OnOkClick" IsDefault="True"/>
            <Button Content="Cancel" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
