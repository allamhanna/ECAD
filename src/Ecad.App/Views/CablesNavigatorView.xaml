<UserControl x:Class="Ecad.App.Views.CablesNavigatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="8">
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
            <TextBlock Text="New cable — Tag:" Margin="0,0,0,4"/>
            <TextBox Text="{Binding NewTag, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Add Cable" Command="{Binding AddCableCommand}" Margin="0,4,0,0" Padding="8,2" HorizontalAlignment="Left"/>
        </StackPanel>

        <DataGrid x:Name="Grid" ItemsSource="{Binding Cables}" AutoGenerateColumns="False" CanUserAddRows="False"
                  IsReadOnly="True" SelectionMode="Extended" SelectionUnit="FullRow"
                  SelectedItem="{Binding SelectedCable}"
                  SelectionChanged="OnSelectionChanged" MouseDoubleClick="OnMouseDoubleClick"
                  PreviewMouseRightButtonDown="OnPreviewRightButtonDown">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tag" Binding="{Binding Tag}" Width="80"/>
                <DataGridTextColumn Header="Type Designation" Binding="{Binding TypeDesignation}" Width="*"/>
            </DataGrid.Columns>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="_Edit Cable..." Click="OnEditCableClick"/>
                    <MenuItem Header="_Manage Cores..." Click="OnManageCoresClick"/>
                    <Separator/>
                    <MenuItem Header="_Delete Selected" Command="{Binding DeleteSelectedCablesCommand}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </DockPanel>
</UserControl>
