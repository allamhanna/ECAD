# Progress

Status legend: `[ ]` not started · `[~]` in progress · `[x]` done

Stack: C#/.NET 8, WPF, SkiaSharp, Svg.Skia, Microsoft.Data.Sqlite (see `DECISIONS.md` ADR-001). PDF/Excel/CSV export (M13) still needs a library choice — QuestPDF was tried for M12's report viewer and removed over its commercial-license terms; see ADR-020.

## Phase 1 — Daily-use internal tool

- [x] **M0** — Toolchain + repo/solution scaffold
  - [x] Install .NET 8 SDK
  - [x] REQUIREMENTS.md / DECISIONS.md / PROGRESS.md / STRUCTURE.md in place
  - [x] git init + .gitignore + initial commit (`main` branch, commit `a33f132`)
  - [x] Solution + projects scaffolded (App/Core/Data/Rendering/Reports + tests)
  - [x] NuGet packages added, project references wired
  - [x] Solution builds cleanly end to end (`dotnet build EcadApp.sln` — 0 errors)
- [x] **M1** — Data model & SQLite layer: schema for Project/Page/Device/Placement/Connection/Cable/Part/Symbol + UDPs, versioned migrations, repository layer
  - [x] `Ecad.Core` domain models, enums, `DeviceTag`/`PageTag` IEC 81346 value objects
  - [x] Project DB + Library DB migrations (0001_initial.sql each), `MigrationRunner`
  - [x] Repositories: Project/Page, Device/DevicePin, Placement/PlacementPin (+ cross-reference query), Connection/ConnectionEnd, Cable/CableCore, Part family (+ upsert-by-ExternalKey), Udp
  - [x] `Ecad.Core.Tests` (8) + `Ecad.Data.Tests` (12) — migrations, cross-reference scenario, termination round-trip, cable/core assignment, UDP attach, Part upsert add/update/no-op — all passing
  - [x] Report/form-layout tables and symbol connection-point geometry deliberately deferred to M12 (Reports)/M4 — milestone numbers shifted twice since this was written as later insertions (M10 shell redesign, then M11 definition-points/cables) pushed the tail milestones back each time, same renumber-and-note precedent both times
- [x] **M2** — App shell & project management: create/open/save project, main window, page list panel
  - [x] `Ecad.Data/ProjectSession.cs` — Create/Open/AddPage/Checkpoint/Dispose, unit tested (4 new tests, 16 total in Ecad.Data.Tests)
  - [x] `Ecad.App`: `MainViewModel` (CommunityToolkit.Mvvm) + `NewProjectDialog`/`AddPageDialog` + rewritten `MainWindow` (menu, page ListView, status bar)
  - [x] Verified the app actually launches (`Ecad.App` process with `MainWindowTitle: ECAD` confirmed via `Get-Process`) — visual/interactive behavior (dialogs, ListView rendering) not verified by me; ask the user to click through New/Open/Add Page
  - [x] Global `DispatcherUnhandledException` handler added (`App.xaml.cs`) — shows a message box instead of failing silently, added while investigating a reported "Open Project does nothing" issue
  - [x] `SaveAs` implemented: `ProjectSession.SaveAs(newPath)` checkpoints, copies the file, and returns a session on the new path (same-path calls just checkpoint, no-op copy); wired to File > Save As... — 2 new tests (18 total in Ecad.Data.Tests)
  - [x] Fixed: Save/SaveAs/CloseProject/AddPage menu items stayed permanently disabled after opening a project — `RelayCommand` doesn't auto-requery on property changes; added `[NotifyCanExecuteChangedFor]` on `IsProjectOpen` so those commands actually re-enable. User-confirmed fixed live.
  - [x] M2 fully confirmed working end-to-end by the user (New/Open/Add Page/Save/Save As/Close all click-tested live)
- [x] **M3** — EPLAN parts import engine (see ADR-004): `EplanEdzImporter` parses the real `.edz` (manifest.xml + per-part XML) into Library DB `Part`/`Organization`/`PartPinTemplate`/`PartAccessory`/`PartTerminalSpec`, upserting by `ExternalKey`. `File > Import EPLAN Parts...` wired up (async, writes to the shared Library DB independent of any open project). 25 tests in `Ecad.Data.Tests` (up from 18), including 4 regression tests for real-data issues only found by running against the actual H2L Robotics file (see log). Manually verified end-to-end: 636 distinct parts imported from the real export in ~5.7s, 0 warnings.
  - Not covered in this pass (see ADR-004/M3 plan for the honest scope line): pictures/documents/macros, classification/ECLASS, multi-variant parts beyond the first variant, full parts-management CRUD UI/classification tree (that's the rest of the original M3 description below, now deferred)
  - [x] Parts Library browser UI: `File > Parts Library...` opens a non-modal window (own Library DB connection) — search/filter over article number/description/manufacturer, master-detail with read-only Pin Templates/Terminal Specs/Accessories grids. Editing, classification tree, and UDP definition UI intentionally deferred (no data populates either yet) — see PROGRESS.md log entry.
  - [x] Part preview images (see ADR-005): new `PartImage` table (BLOB, not a file-path reference — preserves project-file portability), extracted unconditionally on import/re-import so already-imported parts get backfilled too. Parts Library detail panel shows the image. Backfilled the real library: 525/636 parts now have an image, 0 warnings. Caught and fixed a real bug along the way: `UpsertByExternalKey` never set `part.Id` on its `Unchanged` path.
  - [ ] Editable part fields, classification tree UI, UDP definition UI — deferred, no classification/UDP data exists yet to justify the UI
  - [ ] CSV/XML fallback import wizard (Section 6.6) — deferred, `.edz` covers the real use case
- [x] **M4** — Symbol format & starter IEC symbol set, symbol browser panel (see ADR-006): SVG + JSON-sidecar format (connection points, text placeholders, variants), 8 hand-authored starter symbols bundled with the app, `File > Symbol Browser...` shows rasterized thumbnails. New `Ecad.Rendering.Tests` project (6 tests) covers the loader (valid/missing-SVG/malformed-JSON/missing-folder) and rasterizer. Symbol Library table in the DB stays unpopulated until M5/M6 actually need it.
- [x] **M5** — Schematic canvas core: pan/zoom/grid snap, place/select/move/rotate symbols, undo/redo framework
  - [x] `Ecad.Rendering/Canvas/`: `CanvasViewport` (pan/zoom/grid-snap + world↔screen conversions), `PlacementHitTester` (rotation-aware hit test), `UndoRedoStack` (generic `IUndoableCommand` push/undo/redo), `SchematicCanvasRenderer` (pure SkiaSharp drawing — grid, placements, selection highlight, tag text) — 22 new tests in `Ecad.Rendering.Tests`
  - [x] `Ecad.Data`: `PlacementRepository`/`DeviceRepository` gained `GetOrCreateSymbol`, `UpdatePosition`, `UpdateRotation`, `UpdateDeviceTag`, `DeleteDevice`, `GetPlacementsForPage`; `ProjectSession` gained `PlaceSymbol`/`MovePlacement`/`RotatePlacement`/`RenameDevice`/`DeletePlacement`/`GetPlacements` — 5 new tests in `Ecad.Data.Tests` (34 total, up from 29)
  - [x] `Ecad.App`: `SchematicPageViewModel` (viewport/selection/undo-redo, mouse/keyboard → `IUndoableCommand`s), `PlaceSymbolCommand`/`MoveCommand`/`RotateCommand`/`RenameTagCommand`/`DeleteCommand`, `DeviceTagDialog`, `SchematicPageWindow` (first real interactive use of `SkiaSharp.Views.WPF`'s `SKElement` — M4 only ever rasterized static thumbnails). Double-clicking a page in `MainWindow` opens it non-modally.
  - [x] Interaction: click a palette symbol then click the canvas to place it (prompts a device tag); click/drag to select and move (grid-snapped); double-click a placement to rename its tag; `R` rotates 90°, `Delete` removes; right-drag pans, wheel zooms (centered on cursor); Ctrl+Z/Ctrl+Y or toolbar buttons undo/redo
  - [x] Fixed a real crash found by the user during manual testing: caching an `SKPicture` from a `using`-disposed `SKSvg` left the picture pointing at freed native memory — the next repaint after placing a symbol hard-crashed the process with `AccessViolationException` in `sk_picture_get_cull_rect`. Fixed by keeping the `SKSvg` instances alive (disposed with the ViewModel) instead of just their `Picture`.
  - [x] Fixed a real bug found by the user: symbols could be placed but not reliably selected/dragged afterward. Root cause: `SKElement.PaintSurface` reports its canvas size in physical pixels while WPF mouse events report DIP (logical) coordinates, so on any display with scaling ≠ 100% a placed symbol renders at a different on-screen position than where it was clicked — clicking on the visible symbol misses its hit-test box entirely. Fixed with `SKElement.IgnorePixelScaling = "True"`, confirmed via a throwaway reflection probe against the actual `SKElement` type before applying it.
  - [x] Added rename-tag support (double-click a placement) after the user asked to "edit that symbol" — clarified this meant the device tag, not the symbol's geometry (that's the Phase 2 symbol editor, still deferred) or assigning a `Part` to a placement (that's M6 territory). Both clarifications avoided pulling later-milestone scope into M5.
  - [x] Manually verified end-to-end by the user: place, select/drag, rotate, delete, rename, undo/redo all confirmed working live.
- [x] **M6** — Device tagging & cross-references (multi-placement devices, IEC 81346 tags)
  - [x] Multi-placement devices: placing a symbol now offers "New Device" or attaching to an existing Device in the project (new `DevicePin`s created for whatever the symbol exposes, added to that Device) — the M1 schema already supported this (`Placement.DeviceId` was never unique); M5's app layer just always created a new Device, which M6 removes.
  - [x] Segment-aware tag editing: `DeviceTagDialog` upgraded from one free-text field to Function/Location/Designation with a live `=F+L-D` preview (reusing `Ecad.Core.ValueObjects.DeviceTag`, previously dead code outside its own unit tests). Placement mode pre-fills Function/Location from the page's own segments and auto-suggests the next Designation (simple per-Function+Location sequential number, freely editable — see ADR-008 on why this isn't a full numbering-rule engine). Tag uniqueness validated per project before confirming, excluding self on rename.
  - [x] Deletion made placement-level instead of device-level: deleting one placement of a multi-placement Device now removes only that placement and the `DevicePin`s exclusive to it, leaving sibling placements and the Device intact; the Device itself is only deleted once it has no placements left. Undo (`DeleteCommand`) branches accordingly — recreates a whole new Device if it was the last placement, or just a new placement on the same surviving Device otherwise.
  - [x] Cross-reference display: a placement of a multi-placement Device now shows a small `-> Pg n` line under its tag, listing its sibling placements' pages (Section 5.4). Genuinely live across windows — every open `SchematicPageWindow` shares one `ProjectSession`, which now raises a `PlacementsChanged` event after any placement add/attach/delete/rename, so a change in one window's page immediately updates another already-open window showing a sibling placement, no reopen needed.
  - [x] 6 new tests in `Ecad.Data.Tests` (`ProjectSessionMultiPlacementTests`: attach-to-existing-device, delete-keeps-sibling-alive, delete-last-removes-device, tag uniqueness, Designation auto-suggest scoping, sibling label round-trip) — 40 total (up from 34), 70 overall.
  - [x] Fixed a real Dapper materialization bug found while building this (same class of issue as ADR-002 documents, a new variant): a `record`-typed sibling-query result failed with "byte[] required" for the common zero-sibling case, because Microsoft.Data.Sqlite can't resolve a concrete type for a computed `COALESCE`/concat expression column without an actual row to inspect. Fixed by querying into a plain settable-property row class (lenient mapping) and building the label string in C# instead of SQL.
  - [x] Fixed a real bug found by the user during manual testing: deleting a placement didn't clear the survivor's cross-reference text in an already-open window for the sibling's page. Root cause: the live refresh only re-queried the page where the change happened, not other simultaneously-open windows. Fixed with the `ProjectSession.PlacementsChanged` event above.
  - [x] Deferred out of this milestone (confirmed with the user before starting): assigning a `Part` to a `Device` (the `Device.PartId` column stays unused) — likely M8 or its own slice later.
  - [x] Manually verified end-to-end by the user: new-device placement with auto-suggested tag, attaching a second placement to an existing device, live bidirectional cross-reference text (including across two open windows), rename propagating to both placements, delete/undo of a non-last placement, and final delete of the whole device.
- [x] **M7** — Auto-connect wiring: orthogonal routing, junctions, auto wire numbering (plus the cross-page "interruption points" follow-up, completed right after — see the bottom of this checklist)
  - [x] Pin geometry finally used for real: `SymbolConnectionPoint.X/Y/Direction` (parsed since M4, unused until now) transformed by each placement's position/rotation/mirror (`PlacementPinGeometry`) to get every pin's live world position and outward-facing direction. Pins render as small markers on every placement.
  - [x] Manual wire drawing: click-drag from one pin to another draws a live dashed preview and creates a `Connection` on release. Wires have no stored route — `OrthogonalRouter` recomputes a straight-or-one-bend path fresh from both pins' current positions on every render, so a wire visibly re-routes as either endpoint's placement moves, with no extra state to keep in sync.
  - [x] Auto-connect and auto-disconnect, direction-aware (redesigned twice from user feedback during testing): two pins connect only when they sit on the same grid row/column *and* their connection-point directions point at each other (e.g. a pin exiting right only joins another pin further along that row exiting left back toward it) — not merely touching, and not merely sharing a line. This check re-runs after every place/attach/move/rotate: sliding a connected part further along its line keeps the wire (just longer), moving it off that line drops the connection outright, matching a real user expectation that a wire shouldn't just elastically follow a part wherever it goes. A pin landing mid-span on an existing wire's path still auto-joins it too (a T-junction). While a part is being dragged, its wires are hidden entirely rather than stretching live to the cursor, since whether it ends up connected is only decided on drop.
  - [x] Magnetic snap while placing/dragging: pulls the relevant axis (row for a left/right-facing pin, column for up/down) into exact alignment with a nearby *compatible* (opposite-facing) pin — without this, landing the required exact grid alignment by eye is impractical since pin offsets aren't visible before placing.
  - [x] Junctions: a filled dot renders wherever 3+ connections share an endpoint, or one connection's endpoint lands inside another's routed path — computed purely from the existing pairwise `Connection` records (`JunctionDetector`), no separate junction entity needed.
  - [x] Auto wire numbering: sequential, project-wide (one fixed scope, not Section 6.1's "configurable per-page-or-project" toggle — same simplification precedent as ADR-008's Designation suggestion), assigned automatically on every connection creation. Double-click a wire to rename its number (uniqueness-validated); a "Renumber Wires" toolbar button reassigns every connection in the project in page order.
  - [x] Wire select/delete/undo, same shape as placement select/delete from M5.
  - [x] Necessary fix found while building this: `ProjectSession.DeletePlacement` now deletes any `Connection`s touching the placement's pins *before* deleting the pins themselves — `Connection`'s FKs to `DevicePin` have no `ON DELETE CASCADE`, so deleting a wired placement would otherwise throw. Undoing the delete restores the placement but not its former wires (same recreate-not-restore acceptance as ADR-007).
  - [x] `ProjectSession.ConnectionsChanged` event added, exact same shape as M6's `PlacementsChanged`, so wires stay live across every open page window, not just the one where a change happened.
  - [x] 68 new tests: `Ecad.Rendering.Tests` gained `PlacementPinGeometryTests`, `OrthogonalRouterTests`, `AutoConnectDetectorTests`, `JunctionDetectorTests`, `WireHitTesterTests` (58 total, up from 22); `Ecad.Data.Tests` gained `ProjectSessionConnectionTests` (47 total, up from 40). 113 tests passing overall.
  - [x] Manually verified end-to-end by the user across several rounds of live feedback: pin markers, manual wire drawing, auto-connect actually being achievable in practice (not just theoretically exact-match), junctions, wire rename/renumber/delete/undo, and the final auto-disconnect-on-move-away + hide-wires-while-dragging behavior.
  - [x] **Interruption points (cross-page wire continuation) follow-up** — turned out to need no schema/data-layer changes at all: an interruption point is just a new one-pin starter symbol, and *pairing* two instances across pages is exactly M6's existing "attach placement to an existing Device" flow — two placements of one Device, whose cross-reference mechanism (already built in M6) shows a live "→ Pg n" between them with zero new code. Added `InterruptionPoint.svg`/`.symbol.json` with connection points at **both ends** of the arrow (per user feedback) so either end can be wired — the arrow visually points away from or into the real connection depending on which pin you use, no rotation needed. The one genuinely new piece: Ctrl+Click a placement with a sibling raises `SchematicPageViewModel.NavigateToPageRequested`, which `SchematicPageWindow` handles by opening the sibling's page (works for any multi-placement device, not just interruption points, as a natural side effect). Per a second round of feedback, this doesn't just open a new window each time — a small static `Dictionary<PageId, SchematicPageWindow>` registry (`SchematicPageWindow.OpenOrFocus`) means a page only ever has one window open; navigating (or double-clicking the page again from MainWindow, now routed through the same method) brings the existing one to front and selects the target placement instead of duplicating it. No new tests (no Ecad.Data/Core/Rendering changes); existing 113 unaffected.
- [x] **M8** — Grid-based editing: Devices/Connections/Cables grids, bulk edit
  - [x] Confirmed with the user before starting: M8 is CRUD-grid mechanics only for data the schema already supports — the deeper termination workflows in Section 6.3 (filterable bulk-ferrule-assign, stripping-length auto-populate, end-type classification) stay M9's scope, even though `ConnectionEnd` and its termination columns already exist and are already populated by `CreateConnection` since M7.
  - [x] New `GridEditorWindow` (`File > Grid Editor...`), one per open project (`OpenOrFocus` singleton, simpler than `SchematicPageWindow`'s per-page registry since there's only ever one instance): a `TabControl` with Devices/Connections/Cables tabs, each an editable WPF `DataGrid` (this app's first — `PartsLibraryWindow`'s grids were read-only) backed by its own tab view-model.
  - [x] Devices tab: ~~create "bare" Devices with no Placement at all (`ProjectSession.CreateBareDevice` — new territory, `PlaceSymbol` always created Device+Placement together before now) and~~ (creation removed 2026-07-09, see ADR-011 and the log entry below) finally wire up `Device.PartId` (unused since M1, explicitly deferred out of M6 as "likely M8" — see ADR-008). Bulk-assign a Part to multiple selected Devices at once.
  - [x] Connections tab: ~~create/~~(creation removed 2026-07-09, see ADR-011) edit connections by picking from/to pins (flattened Device+Pin combo), plus color/cross-section/length/Cable/CableCore fields — deliberately no termination columns (M9). Bulk-set color or cross-section across a multi-selection. `CableCoreId`'s combo is filtered per-row to the selected Cable's own cores via a `DataGridTemplateColumn` + `IMultiValueConverter`, since a plain `DataGridComboBoxColumn` can't filter its item source per row.
  - [x] Cables tab: create/edit Cables and their CableCore rows (master-detail, same visual shape as `PartsLibraryWindow`'s nested sub-grids but editable). Real schema gap found and fixed: `Cable` had no `ProjectId` column at all — migration `0003_cable_project_id.sql` adds it (nullable, no data to backfill since `CableRepository.InsertCable` had never been called from any wired-up path before this). Join-through-`Connection` was considered and rejected for project-scoping since Section 5.6 allows a Cable to exist as pure data with zero Connections.
  - [x] Delete-guard asymmetry (ADR-010): deleting a Cable still referenced by a Connection is rejected outright; deleting a CableCore instead auto-clears the `CableCoreId` on any referencing Connection (leaving `CableId` intact) and proceeds — a core is a much smaller, easily-re-picked assignment than a whole cable's identity.
  - [x] No undo/redo for grid edits (ADR-010) — `UndoRedoStack` is per-`SchematicPageWindow` and grids must work with zero pages open. Non-destructive edits commit immediately like the canvas; destructive grid actions (delete Device/Connection/Cable/CableCore) get a Yes/No confirmation dialog instead, an explicit stated gap against Section 7's "all destructive operations undoable" rather than a silent one.
  - [x] `ProjectSession.CablesChanged` event added, identical shape to `PlacementsChanged`/`ConnectionsChanged` — the Grid Editor's tabs and the Connections tab's Cable/CableCore pickers stay live off the same shared-event pattern (ADR-008/009).
  - [x] 23 new tests in `ProjectSessionGridEditingTests` (bare-device lifecycle, DevicePin add/update/delete-when-wired-throws, project-wide `GetAllConnections`, endpoint rewire, single/bulk color+cross-section updates, Cable project-scoping across two separate project files, orphan-cable-with-no-connections listing, Cable delete-guard, CableCore delete auto-clear, `CablesChanged` firing) plus a migration test asserting the new `Cable.ProjectId` column exists — 137 tests passing total (8 Core + 58 Rendering + 71 Data). *(Test count later dropped to 136 when grid-creation was removed — see the 2026-07-09 log entry below.)*
- [x] **M9** — Cable & termination model: per-end termination toggle/parts, filterable bulk-assign
  - [x] Confirmed with the user before starting: three REQUIREMENTS-mentioned "auto-populate/derive" pieces (stripping length from part data, cable cores from a cable Part's own core template, `Cable.EndTypeClassification` derived from its cores' terminations) have no real data source anywhere to drive them — all three stay manual-entry-only. M9 focuses on the well-specified core: per-end termination toggle/type/part (manual), and the filterable bulk-assign view (Section 6.3's own example).
  - [x] New 4th tab on the Grid Editor, "Terminations": shows every `ConnectionEnd` in the project (two per Connection — From/To), with the parent Connection's wire number/cross-section for context, inline-editable Termination Enabled/Type/Stripping Length, and a read-only assigned-Part column (same split as `Device.PartId` since ADR-012 — assignment is bulk-toolbar-only). `ConnectionEnd` rows are never independently created or deleted (always exactly two per Connection, cascade-deleted with it) — this tab only edits existing rows, same spirit as ADR-015's decision for Connections themselves.
  - [x] Filtering is in-memory (cross-section, termination type, "unassigned only" checkbox) over rows already loaded — no re-query per filter change, matching how every other M8 tab avoids per-filter database round-trips.
  - [x] Bulk-assign termination Part reuses `ProjectSession.EnsurePartCached` as-is (ADR-012's Library→project Part-caching, not Device-specific) — new `ProjectSession.BulkUpdateConnectionEndPart`/`ConnectionRepository.UpdateConnectionEndPart`, same once-per-batch `ConnectionsChanged` convention as `BulkUpdateDevicePart`.
  - [x] New `ConnectionEndWithContext` Core model + `ConnectionRepository.GetAllConnectionEndsWithContext` (project-scoped join of `ConnectionEnd`+parent `Connection`, using the established lenient-record Dapper workaround for joined/computed columns — ADR-002/ADR-008) — endpoint labels are resolved in the ViewModel from `GetAllDevices()`/`GetDevicePins()`, not baked into the SQL.
  - [x] No new architectural pattern — reuses every M8 pattern as-is (flattened read-shape row `TerminationRow`, purpose-built toolbars, `EnsurePartCached`, `ConnectionsChanged`/`PlacementsChanged` fan-out, `CanUserAddRows="False"` + `OfType<T>()` selection handling per ADR-014). No new ADR.
  - [x] 9 new tests in `ProjectSessionGridEditingTests` (termination field round-trip + clear, wire-number/cross-section carried from the parent Connection, project-scoping across pages, both End designators present per Connection, `EnsurePartCached` reused for a termination part with no FK violation, bulk-assign applies only to targeted ends and raises `ConnectionsChanged` once, bulk-clear, and a regression guard that `CreateConnection` still creates both ends with termination off) — 149 tests passing total (8 Core + 58 Rendering + 83 Data, up from 140). Build succeeded and all tests passed on the first real attempt (no bugs found yet) — unlike M8, still pending a live click-through before calling it fully confirmed.
- [x] **M10** — Application shell: single tabbed workspace (replacing one-floating-Window-per-document), docked device-properties panel, recent files/auto-reopen (inserted ahead of M11/M12/M13 per explicit user direction — see the 2026-07-10 log entry and DECISIONS.md ADR-016/ADR-017)
  - [x] Phase 1 — `MainWindow` restructured to one window (Pages sidebar + document `TabControl`), `DocumentTabViewModel` wraps each open document, implicit `DataTemplate`s resolve a tab's View from its ViewModel's type, `SchematicPageWindow` extracted into `SchematicPageView` (`UserControl`) and the old per-page window registry deleted; Ctrl+Click cross-page navigation now finds-or-creates a page tab instead of opening a window
  - [x] Grid rendered as dots (adjustable density via a `GridSpacing` combo box), not lines — existing placements are unaffected since `GridSpacing` only ever changes rendering/snap density, never a placement's stored world position
  - [x] Auto-reopen the last project on launch (unless explicitly closed last session) and open its first page automatically; drag-and-drop a symbol from the palette straight onto the canvas (alongside the existing click-to-place flow)
  - [x] Phase 2 — `GridEditorWindow` extracted into `GridEditorView`, opened as a singleton project-scoped tab (found-or-created by ViewModel type, like the page tabs' PageId-keyed lookup)
  - [x] Phase 3 — `PartsLibraryWindow`/`SymbolBrowserWindow` extracted into `PartsLibraryView`/`SymbolBrowserView`, each a singleton project-*independent* tab (`IsProjectScoped = false`, survives Close Project, same as the old windows never holding a `ProjectSession`)
  - [x] Selection/pointer model, settled after two follow-up rounds of live feedback: **left-click-drag** on empty space is the rubber-band multi-select (armed on button-down, only becomes an active marquee past a small pixel threshold, so a stationary click still selects a wire/deselects exactly as before) — direction-aware AutoCAD-style, dragging left-to-right requires a placement to be *fully enclosed* ("window" select), right-to-left only requires *touching* the box ("crossing" select); **right-click** selects whatever's under the cursor and opens a context menu (Rotate/Delete/Undo/Redo, sharing `DeleteSelectionCommand`/`RotateSelectionCommand` with the `Delete`/`R` keyboard shortcuts); **middle-click-drag** pans. Multi-selected placements support group move (relative offsets preserved, snapped as one shared delta) and group delete, both as one undo/redo step (`CompositeCommand`) — Rotate and wire/connection selection stay single-select only. The canvas now captures the mouse during any left/middle-button drag so it keeps tracking correctly even if the cursor leaves the canvas bounds (matching EPLAN's own drag behavior), with a defensive cancel-and-revert if capture is ever lost mid-drag (e.g. focus stolen by another window)
  - [x] Phase 4 — docked, non-modal device-properties panel (Function/Location/Designation + live tag preview + inline validation) replacing the modal `DeviceTagDialog` for the rename (double-click-an-existing-placement) path only; new-placement's tag prompt and wire-rename both stay their existing modal dialogs, per the original plan
- [x] **M11** — Wire & cable definition points: explicit connection-definition-point placement (replacing M7's automatic wire numbering), redesigned into an independent entity after a real "disappears on symbol move" bug, plus a cable-definition-line feature (draw a line across wires to auto-assign a cable's cores) — inserted ahead of M12/M13/M14 (Reports/Export/Hardening, renumbered from M11/M12/M13) per explicit user direction, same insert-and-renumber precedent M10 already used (see the 2026-07-11/12 log entries and DECISIONS.md ADR-018/ADR-019)
  - [x] Connection definition points (initial build): a wire now renders as a plain line until the user explicitly places a definition point on it (a diagonal tick, toggled via a "Place Definition Point" toolbar tool) — carries WireNumber/Color/CrossSectionMm2 via `DefinitionPointDialog` (place/edit/remove), replacing M7's always-on automatic numbering. Can be dragged along its own wire or relocated to a different one.
  - [x] Selection-model fix: extended ADR-015's "a connection has no independent identity" principle from the Grid Editor to the canvas — a wire itself is never selectable/deletable, only its definition point is. Fixed a real bug where double-clicking a bare wire could still select the underlying `Connection` and let Delete remove the whole wire; rubber-band selection extended to include definition-point ticks alongside placements.
  - [x] Root-cause redesign: moving *any* symbol connected to a defined wire made its definition point silently vanish, because M7's auto-connect deletes and recreates the underlying `Connection` row on nearly every move — and the definition point's data lived on that row. Redesigned as a fully independent `DefinitionPoint` entity (own table, own `PageId`, absolute X/Y, optional `ConnectionId` with `ON DELETE SET NULL`) that survives its attached connection being deleted/recreated, mirroring Wire#/Color/CrossSectionMm2 onto the Connection's own columns only while attached — Grid Editor's Color/Cross-section columns become read-only for an attached row, the same treatment Wire# already got. See ADR-018.
  - [x] Follow-ups found via live testing: a grid-snap bug (dragging a definition point wasn't snapping to the grid at all); rotation (R key, 90° per press, matching symbol rotation) and a red default tick color (DodgerBlue still highlights when selected).
  - [x] Cable definition lines: draw a straight line across one or more wires (a new toolbar toggle) to assign them to a Cable — no upfront cable/core setup required. Finds-or-creates the Cable by tag, auto-creates a sequentially-numbered `CableCore` per crossed wire, and mirrors the assignment onto `Connection.CableId`/`CableCoreId` via the existing M8 write path. New `CableLine`/`CableLineCrossing` tables reuse the same independent-entity/`ON DELETE SET NULL` lesson `DefinitionPoint` just learned, plus a new `SegmentIntersection` geometry primitive (segment/polyline crossing — nothing like it existed before). Resolves ADR-011's "revisit once cable-definition-line drawing is built" note: Grid-based Cable creation stays (a Cable can validly exist as pure data), the canvas becomes the normal way core-to-wire assignments actually get made — see ADR-019.
  - [x] Cable line interaction, refined over several rounds of live feedback: drag the whole line to move it (re-detects crossings) or drag either endpoint independently to extend/shrink it; double-click to re-home to a different cable or remove it entirely. Each crossing tick is independently selectable (click/right-click/rubber-band), rotatable (R key), and double-click-editable (core number/color/cross-section, with a uniqueness check) via a new `CableCoreDialog`, rendered with the same red diagonal-tick glyph as a wire definition point (was a plain circle). A grid-snap bug in whole-line dragging (same class of bug already fixed once for definition points) was found and fixed; drawing a line that crosses zero wires is cancelled outright, since a cable line's entire purpose is crossing wires (unlike a definition point, which can validly be free-floating).
  - [x] 196 tests passing total (8 Core + 86 Rendering + 102 Data, up from 155) — new `SegmentIntersectionTests`/`CableLineHitTesterTests`, extended `WireHitTesterTests`, and `Ecad.Data.Tests` coverage for definition points (place/move/attach/detach/delete, orphaning on connection delete) and cable lines (core auto-numbering, idempotent re-draw, conflict-skip, mirror-clear on delete, re-homing, rotation, core-edit uniqueness). No UI/canvas-interaction automated tests (established project-wide pattern) — every interaction was live click-tested and iterated on with the user across several rounds of feedback.
- [x] **M12** — Reports engine: form/report JSON layout schema + the 4 Phase-1 reports (connection list, BOM, cable overview, cable manufacturing sheet)
  - [x] Declarative JSON layout schema (Section 5.9) in `Ecad.Reports.LayoutSchema`: `ReportLayout`/`PageSetup`/`HeaderFooterRegion`/`PageBreakRule` plus a closed `LayoutRegion` hierarchy (`StaticTextRegion`/`FieldRegion`/`DrawingAreaPlaceholder`/`RepeatingTableRegion`) deserialized via `System.Text.Json`'s built-in `[JsonPolymorphic]`/`[JsonDerivedType]` type-discriminator support. `ReportLayoutLoader.LoadFromFolder` mirrors `SymbolLibraryLoader`'s shape (and its "collect warnings, don't crash on one bad file" tolerance) over 5 hand-authored templates in `Ecad.App/ReportTemplates/*.json` — loose files, editable without a rebuild, matching "editable as files in Phase 1."
  - [x] Four report Builders (`Ecad.Reports/Builders/`), each a pure data-in/document-out transform never touching SQL: `ConnectionListReportBuilder`, `CableOverviewReportBuilder`, `CableManufacturingSheetReportBuilder` (variant-selected by `Cable.EndTypeClassification`, e.g. FER-FER vs FER-CONN, falling back to a default layout with a warning rather than failing), and `BomReportBuilder` (three grouping modes — Project/Location/Cable-assembly; the "no double counting" rule from Section 6.3/6.4 is a concrete exclusion: a cable's own Part plus its wires' termination Parts are counted once inside that cable's module and explicitly excluded from the flat top-level totals).
  - [x] A report becomes a Page via a new `GeneratedReport` identity table (`ReportKind`, `SourceEntityId`, `GroupingKey`) → `Page`, looked up by that identity so re-running a report reuses the same Page/tab rather than duplicating it (Section 6.4's "no page-number collisions"), with its own `DocumentTypeSegment` per kind (`RPT-CONN`/`RPT-BOM`/`RPT-CAB`/`RPT-F09`). A report page caches nothing — opening its tab and clicking "Regenerate" are the same code path (re-run the Builder against current session data), so there's no separate stale-cache problem. `PageType.Report` turned out to already exist in the enum, unused anywhere — this is its first real use. First-ever `Page` update/delete path added (`ProjectRepository.UpdatePage`/`DeletePage`), needed for manufacturing-sheet cleanup when its Cable is deleted or renamed away (`ProjectSession.DeleteCable` extended; `DeleteOrphanedCableManufacturingSheets` batch-cleans the rest on each "Generate All Manufacturing Sheets" run).
  - [x] New `_Reports` menu on `MainWindow` (nothing else to hook a Reports feature into yet — no Export menu exists, that's M13): Connection List / BOM (opens a grouping-choice dialog) / Cable Overview / Cable Manufacturing Sheets (batch, one page per Cable) / Regenerate All Reports (simply re-triggers every currently-open report tab's own Regenerate, since nothing is cached). A new, much smaller `ReportPageView`/`ReportPageViewModel` (no canvas, no undo stack) shows the Builder's output as plain header-field text plus an ordinary WPF `DataGrid` — deliberately not a repurposed `SchematicPageView`, which is a full SkiaSharp graphics editor a tabular report has no use for.
  - [x] **Mid-build correction**: the first pass built exactly what was planned — QuestPDF rendering a rasterized PDF preview into the report tab, per ADR-001's original stack pick — and it worked (built clean, all tests passing) before the user stopped it: reports should be a plain in-app data page auto-generated from the drawings, not a PDF, since QuestPDF carries a real commercial-license cost the user doesn't want tied to the core report-viewing path right now; PDF/Excel/CSV export stays a deliberately deferred "for later" concern (M13), with the export technology now an open decision for that milestone rather than one baked in silently here. QuestPDF was removed from `Ecad.Reports` entirely the same day (the `PackageReference`, and the whole QuestPDF-based `Rendering/` folder) — the JSON layout schema and all four report Builders needed zero changes, since they were always pure data transforms with no rendering dependency; only `ReportEngine` (shrank to template loading/lookup only) and `ReportPageViewModel`/`ReportPageView` (rewritten to expose/display plain header fields + table rows via a `DataGrid` instead of rasterized images) changed. See ADR-020's revision note.
  - [x] New `Ecad.Reports.Tests` project (15 tests: template-loading tolerance including the polymorphic JSON discriminator, each Builder's data-assembly logic, and the BOM "no double counting" rule specifically) plus `ProjectSessionGeneratedReportTests` in `Ecad.Data.Tests` (regeneration reuses the same Page — no duplicates —, cable-delete cleanup, orphan batch cleanup) — all 15 Reports tests kept passing unchanged through the QuestPDF removal, since none of them touched the (now-deleted) rendering layer. 220 tests passing total (8 Core + 86 Rendering + 111 Data + 15 Reports, up from 196); build and full test suite passed clean both before and after the pivot. No live bugs found yet (build/test-verified only; a live click-through of the Reports menu and DataGrid rendering is still pending the user).
- [ ] **M13** — Export: PDF and Excel/CSV (technology choice, including whether QuestPDF's license terms are acceptable, deferred to when this milestone starts — see ADR-020)
- [ ] **M14** — Non-functional hardening: autosave/crash recovery, perf pass (500+ elements/page), keyboard shortcuts

## Phase 2 — Robustness & authoring tools (backlog, not yet milestoned)
- In-app visual form/report layout editor
- In-app visual symbol editor
- AI-assisted symbol authoring (generate SVG + connection points from a description/screenshot;
  bounded library of device *kinds*, not per-part; possible open-library seed pending license check)
- Rule-based numbering schemes
- Project templates; lightweight revision/change tracking
- Terminal diagrams, wiring lists
- DXF export (optional)

## Phase 3 — Productization (backlog, not yet milestoned)
- Installer/updater, offline-capable licensing/activation
- Crash-safe autosave & project file integrity (backup/restore)
- Documentation, onboarding samples

## Log

- **2026-07-05** — Project folder created (`Documents\electrical CAD`). Requirements reviewed, stack decided (ADR-001), milestone roadmap defined, M0 started: .NET 8 SDK installed via winget.
- **2026-07-05** — M0 completed: solution `EcadApp.sln` scaffolded with `Ecad.App` (WPF), `Ecad.Core`, `Ecad.Data`, `Ecad.Rendering`, `Ecad.Reports`, plus `Ecad.Core.Tests`/`Ecad.Data.Tests`. NuGet packages added (SkiaSharp.Views.WPF + Svg.Skia on Rendering, QuestPDF on Reports, Microsoft.Data.Sqlite on Data). Project references wired per dependency direction in STRUCTURE.md. Full solution builds with 0 errors (some expected NU1701 compatibility warnings from SkiaSharp.Views.WPF/OpenTK targeting older .NET Framework TFMs — functional, revisit if it causes runtime issues once the canvas work starts in M5). Git repo initialized, initial commit `a33f132` on `main`. Next: M1 (data model & SQLite layer).
- **2026-07-05** — User revealed a real EPLAN parts export (`H2L Robotics/parts.edz`, ~236MB) and asked to migrate it in rather than rebuild a parts DB by hand. Inspected a disposable copy (original untouched): it's a 7z archive of per-part native XML (article data, pin templates, precise terminal/torque specs), not a binary DB — far richer than plain CSV. Symbol macros (`.ema`) turned out to be an unpublished internal EPLAN object-model dump, not practically convertible — M4 still ships hand-built SVG symbols. Recorded as ADR-003; folded into M1 schema design (`ExternalKey`, `SourceLastModifiedUtc`, `SourceImportBatchId`, `PartPinTemplate`, `PartTerminalSpec`).
- **2026-07-05** — M1 completed: full schema (Project DB + Library DB) via versioned SQL migrations, Dapper-based repositories (ADR-002), domain models/value objects in `Ecad.Core`. 20 tests passing across `Ecad.Core.Tests`/`Ecad.Data.Tests`, covering the multi-placement cross-reference mechanism, connection termination, cable/core assignment, UDPs, and Part upsert-by-`ExternalKey` (add/update/no-op). Whole-solution build still clean. Next: M2 (app shell & project management) or M3 (EPLAN parts import) — to be decided with the user.
- **2026-07-05** — M2 completed: `ProjectSession` (Ecad.Data) wraps create/open/add-page/checkpoint/dispose, tested (16 tests in Ecad.Data.Tests now, up from 12 — caught and fixed a real connection-leak bug in `ProjectSession.Open`'s error path along the way). `Ecad.App` got a real `MainViewModel` (CommunityToolkit.Mvvm), `NewProjectDialog`/`AddPageDialog`, and a rewritten `MainWindow` with a File menu, page `ListView`, and status bar. Confirmed the app process actually launches (`Get-Process` showed `Ecad.App` with `MainWindowTitle: ECAD`); visual/interactive correctness of the dialogs and list still needs a human click-through since I can't see or drive a rendered window myself. 24 tests passing total, solution builds clean. Next: M3 (EPLAN parts import) most likely, or M5 (schematic canvas) — to be decided with the user.
- **2026-07-05** — User reported "Open Project doesn't lead to anything" while testing M2 live. Added a global `DispatcherUnhandledException` handler (`App.xaml.cs`) so failures show a message box instead of silently doing nothing, to help diagnose. Separately, user asked to implement Save — clarified that "New Project" already saves immediately (autosave-style, no dirty state), so the missing piece was "Save As"; implemented `ProjectSession.SaveAs` (checkpoint + file copy + reopen on the new path, same-path calls are a no-op copy) wired to a new File > Save As... menu item, with 2 new tests (18 total). Still waiting on user confirmation of whether the Open Project issue is resolved.
- **2026-07-05** — Root cause of the "grayed out forever" issue found: it wasn't Open Project failing, it was Save/SaveAs/CloseProject/AddPage never re-enabling after `IsProjectOpen` flipped true, because CommunityToolkit.Mvvm's `RelayCommand` doesn't auto-requery on property changes. Fixed with `[NotifyCanExecuteChangedFor]` on `IsProjectOpen`. User confirmed working live. **M2 fully closed out.** Next: M3 (EPLAN parts import) or M5 (schematic canvas) — asking the user which to do first.
- **2026-07-05** — M3 (EPLAN parts import) chosen and completed. Added SharpCompress (ADR-004) to read the `.edz`/7z archive; `EplanEdzImporter` parses `manifest.xml` + per-part XML into `Part`/`Organization`/`PartPinTemplate`/`PartAccessory`/`PartTerminalSpec`. Built 21 tests against synthetic zip fixtures first — all passed first try — then ran it against a disposable copy of the real `H2L Robotics/parts.edz`, which surfaced 4 real-data issues no fixture had exercised: SharpCompress reports entry keys with forward slashes (not the backslashes `7z l` displays), duplicate keys at both the archive and per-part `<items>` level, `<terminalPosition>` elements with no `name` attribute silently becoming a null that only failed as a SQLite constraint violation at insert time, and one malformed part previously able to abort the entire import and silently drop everything after it in the manifest. Fixed all four with regression tests (25 total). Clean re-run against the real file: **636 distinct parts imported in ~5.7s, 0 warnings.** `File > Import EPLAN Parts...` wired up in the app (async, writes to the shared Library DB). Parts management UI (browsing/editing what got imported) is explicitly not built yet — next milestone territory.
- **2026-07-05** — Parts Library browser UI built: `File > Parts Library...` opens a non-modal `PartsLibraryWindow` (own Library DB connection, independent of any open project — same pattern as the import command) with search/filter and a master-detail view (list + read-only Pin Templates/Terminal Specs/Accessories grids). User chose read-only browse (no editing) and to skip classification tree/UDP definition UI since nothing populates either table yet. Added `PartRepository.GetAllParts`/`GetAllOrganizations` with 2 new tests (27 total in Ecad.Data.Tests, 35 overall). Manual click-through against the real 636-part library still pending user confirmation.
- **2026-07-05** — User asked for a visual (image) preview in the Parts Library. Recognized this as a real architecture decision, not just a UI tweak: storing extracted images as loose files with a path reference would break the "project file is a single portable SQLite file" principle from ADR-003. Decided (ADR-005) to store images as BLOBs in a new `PartImage` table (migration 0002, both Project and Library DBs). `EplanEdzImporter` now extracts images unconditionally (not gated on Added/Updated/Unchanged), which was necessary to backfill the 636 already-imported parts — and which surfaced a real bug: `UpsertByExternalKey` never set `part.Id` on its `Unchanged` return path, silently writing backfilled images against `PartId = 0`. Fixed with a regression test. Re-ran the importer against a disposable copy of the real `parts.edz`: **525 of 636 parts backfilled with an image, 0 warnings.** Parts Library detail panel now shows the image. 37 tests passing total (29 in Ecad.Data.Tests, up from 27).
- **2026-07-05** — Added `.claude/settings.json` allow-rules for `dotnet *`/`git *`/app-lifecycle PowerShell commands (Get-Process/Stop-Process/Start-Process/Start-Sleep/Test-Path/Get-ChildItem) to cut down on repeated permission prompts for this project's normal workflow — deliberately left `rm`/`cp` off the blanket allow-list given their destructive potential.
- **2026-07-05** — M4 (Symbol format & starter IEC symbol set) chosen and completed. Designed the symbol format as plain SVG + a `.symbol.json` sidecar (connection points/text placeholders/variants), all sharing a `0 0 40 40` viewBox convention, recorded as ADR-006. Rasterization for the browser panel uses plain SkiaSharp + Svg.Skia (no WPF dependency) rather than the interactive `SkiaSharp.Views.WPF` control, deferring that to M5 — verified the actual `SKSvg`/`SKPicture` API against a throwaway probe first, having been burned guessing NuGet APIs wrong in M3. Authored 8 starter IEC-style symbols (coil, NO/NC contacts, terminal, 3-phase motor, pushbutton, fuse, lamp), bundled with the app via `CopyToOutputDirectory`. New `Ecad.Rendering.Tests` project (6 tests, the first for that project since M0). `File > Symbol Browser...` shows rasterized thumbnails; 43 tests passing total. Symbol placement/rotation/wiring is explicitly out of scope until M5/M6.
- **2026-07-05/06** — User considered pulling a symbol-editor milestone forward ahead of M5, then explicitly said to keep the milestone order as planned. Separately looked at reusing real EPLAN symbol libraries (`.sdb` folders, then a `.esl` export) to avoid hand-building symbols — both formats turned out to be proprietary/opaque on inspection (same conclusion as `.ema` macros in ADR-003), so M5 proceeds with the M4 hand-built SVG starter set. M5 (schematic canvas core) chosen as next and completed — see the M5 checklist above and ADR-007 for the undo-of-delete design and the two real bugs (SKSvg lifetime crash, DPI hit-test mismatch) found via live user testing and fixed. 64 tests passing total (8 Core + 34 Data + 22 Rendering). User separately mentioned wanting EPLAN-style auto-connect wiring and being able to assign Parts to placements — both acknowledged and deferred to their already-planned milestones (M7, M6) rather than folded into M5. Next: M6 (device tagging & cross-references) or M7 (auto-connect wiring) — to be decided with the user.
- **2026-07-06** — M6 (device tagging & cross-references) chosen and completed — confirmed with the user first that Part-assignment-to-Device stays deferred, keeping M6 focused on what PROGRESS.md actually scoped it as. See the M6 checklist above and ADR-008 for the auto-numbering/deletion-semantics/live-cross-reference design and the three real issues found via live user testing and fixed (a Dapper record-materialization failure on the sibling query, cross-reference text not updating after a delete, and — once diagnosed — the `ProjectSession.PlacementsChanged` event that fixed it for good across all open windows, not just the one where the change happened). 70 tests passing total (8 Core + 40 Data + 22 Rendering). While testing, the user asked about EPLAN-style "interruption points" (a wire endpoint that jumps to where it continues elsewhere) — identified as M7 territory (it's a `Connection`-routing concept, same cross-reference-navigation shape M6 just built but for wires instead of devices) and noted for when M7 is planned, not built now. Next: M7 (auto-connect wiring, now including interruption points) or M8 (grid-based editing) — to be decided with the user.
- **2026-07-06/07** — M7 (auto-connect wiring) chosen. Confirmed with the user first that cross-page "interruption points" would be a focused follow-up after this pass, not part of it, since it depends on the same-page wiring foundation. Built pin geometry, manual wire drawing, live-recomputed orthogonal routing, junction rendering, and project-wide sequential wire numbering — see the M7 checklist above and ADR-009. The auto-connect rule itself went through two rounds of live user feedback before landing: the first version only connected pins at exact grid coincidence, which turned out to be nearly unachievable by eye (fixed with magnetic pin-snap on placement/drag, plus a duplicate-connection-on-create bug and a Dapper-adjacent reentrant-event bug found along the way); the user then asked for the *real* rule — pins on the same grid line whose directions point at each other, not just anything that happens to touch — which is what shipped, plus a symmetric auto-*disconnect* when a move breaks that condition (sliding a connected part further along its line keeps the wire; moving it off the line drops it). A final round of feedback removed a "residue" behavior where a dragged part's wires visually stretched to follow the cursor before the connect/disconnect decision was actually made on drop — wires now hide entirely for the duration of a drag and reappear in their correct final state. 113 tests passing total (8 Core + 58 Rendering + 47 Data). Next: M8 (grid-based editing) or the deferred interruption-points follow-up — to be decided with the user.
- **2026-07-07** — Interruption-points follow-up chosen and completed right after M7. Turned out to need no schema or data-layer work at all — an interruption point is a new one-pin symbol, and pairing two instances across pages is exactly M6's existing "attach to an existing Device" flow, whose cross-reference mechanism already shows a live page reference between them for free. Added `InterruptionPoint.svg`/`.symbol.json`; per user feedback, gave it connection points at both ends of the arrow (not one) so either end can be wired without needing to rotate the symbol. Added Ctrl+Click navigation to a placement's sibling page (`SchematicPageViewModel.NavigateToPageRequested`) — works for any multi-placement device, not just interruption points. A second round of feedback asked that this not open duplicate windows; added `SchematicPageWindow.OpenOrFocus`, a small static page-to-window registry that both this and MainWindow's existing double-click-a-page now route through, so a page only ever has one window open, brought to front and re-selected on repeat navigation instead of duplicated. No schema/Ecad.Data/Core/Rendering changes, so the existing 113 tests are unaffected. Next: M8 (grid-based editing) — to be decided with the user.
- **2026-07-07** — User asked when symbol editing was scheduled, then proposed a new requirement: an AI-assisted way to generate new symbols (from a description, a screenshot, or structured data) instead of hand-drawing every one, plus investigating an existing open symbol library as a possible seed. Logged into `REQUIREMENTS.md`/`PROGRESS.md`'s Phase 2 backlog, reframed during discussion: symbols represent device *kinds* (relay coil, contact, motor...) not individual parts, so this is a bounded library (dozens–hundreds of shapes) rather than a per-part generation pipeline — description/screenshot-driven authoring is already possible informally in any session (same as how the M4 starter symbols were made); a database-driven rules engine or an open-library import would each be their own scoped Phase 2 feature, not built now. No code changes. Continuing with M8 (grid-based editing) next.
- **2026-07-07/08** — M8 (grid-based editing) chosen and completed. Confirmed with the user first that M8 stays CRUD-grid-only, leaving M9's termination/end-type-classification workflows untouched even though the underlying `ConnectionEnd` table already exists. Built a single `GridEditorWindow` (Devices/Connections/Cables tabs) rather than three separate windows, since the grids are cross-referential (Connections needs Device+pin and Cable+core pickers). See the M8 checklist above and ADR-010 for the full design: the `Cable.ProjectId` schema gap found and fixed via migration `0003`, grid-created "bare" Devices, the Cable-reject-vs-CableCore-auto-clear delete asymmetry, and the explicit no-undo-for-grid-edits decision (confirm-before-delete instead). 137 tests passing total (8 Core + 58 Rendering + 71 Data, up from 113). Not yet click-tested live by the user — as with every canvas/window milestone, I can't drive a rendered WPF window myself, only confirm it builds and the unit-testable logic behind it is correct.
- **2026-07-09** — User asked if the app had an icon yet — it didn't (default WPF icon, no `ApplicationIcon` set). Generated a placeholder programmatically (`src/Ecad.App/Assets/AppIcon.ico`, multi-size): a wire/pin/junction motif matching the schematic canvas's own rendering language, since no hand-designed brand icon exists yet. Wired into `Ecad.App.csproj`'s `<ApplicationIcon>` and `MainWindow.xaml`'s `Icon` attribute. Purely cosmetic, no functional change — free to replace with a real design later.
- **2026-07-09** — User clarified a real product direction while discussing M8's requirements: Devices/Connections should always be *generated from the graphics*, not created directly in a grid and somehow made to "become" graphics later — deliberately mimicking EPLAN's canvas-is-the-source workflow for now, with the door left open to reverse this later. Removed the "Add Device" (bare device, no Placement) and "Add Connection" (pin-picker, no drawing) creation UI from M8's Grid Editor, and deleted `ProjectSession.CreateBareDevice` outright (unused once its only caller was gone). Kept the Cables tab's "Add Cable" as the one exception, confirmed with the user — there's no canvas-side way to create a Cable at all yet (the cable-definition-line feature from Section 6.1 is unbuilt), so grid creation is the only path until that exists. See ADR-011 (amends ADR-010) and the updated REQUIREMENTS.md 6.2 note. `CanDeleteDevice`/`DeleteDevice` were kept as a dormant cleanup path rather than removed, since `DeletePlacement` already auto-deletes a Device once its last Placement is gone — a zero-Placement Device should no longer occur in normal use, but the guard costs nothing to keep for legacy data. 136 tests passing (one test removed — `CreateBareDevice_HasNoPlacement` — with three others rewritten to set up their Devices via `PlaceSymbol` instead of the removed method).
- **2026-07-09** — Relaunched the app for the user to test M8 live; they immediately hit a real crash on "Bulk-assign Part to selected": `SQLite Error 19: FOREIGN KEY constraint failed`. Root cause: `Device.PartId` references this *project's own* cached `Part` table (ADR-003's designed-since-M1 "project keeps a local copy of any referenced Part," never actually implemented until now), but the Devices grid's Part picker sourced Library-DB `Part.Id`s and wrote them straight into `Device.PartId` — a foreign key to a row that doesn't exist in the project's database at all. Fixed with `ProjectSession.EnsurePartCached` (copies the Part — and resolves its Manufacturer/Supplier Organization by ExternalKey — into the project DB, returning the project-local Id `UpdateDevicePart`/`BulkUpdateDevicePart` actually need), and changed the grid's Part column to read-only (a two-way-bound combo can't reconcile the Library-id-for-picking vs. project-local-id-for-storage mismatch without a much heavier converter layer) — assignment now goes exclusively through "Bulk-assign Part to selected" (works fine for one row too). See ADR-012. 4 new tests, including a direct regression test reproducing the exact reported crash — 140 tests passing total (8 Core + 58 Rendering + 74 Data).
- **2026-07-09** — User reported the Devices tab's "Delete Selected" does nothing. Root cause: a consequence of ADR-011 that wasn't followed through — since bare Devices can no longer be created, every Device visible in the grid always has at least one Placement, so `CanDeleteDevice` (zero-Placements-only) is now always false for anything selectable, and the button silently no-op'd instead of explaining why. Fixed (ADR-013) by gating the command's enabled state on "at least one selected item is actually deletable" instead of "anything is selected" — the button now disables outright rather than doing nothing when clicked — and pre-emptively applied the identical fix to the Cables tab's "Delete Selected" (same shape, not yet hit live but clearly the same bug class). App-layer-only change; no new automated tests (this app has none for the UI layer), verification is a live click-through.
- **2026-07-09** — User hit a crash multi-selecting rows on the Connections tab: `InvalidCastException: Unable to cast object of type 'MS.Internal.NamedObject' to type 'Connection'`. Root cause: none of M8's four grids set `CanUserAddRows`, which defaults to true — every `DataGrid` has an invisible "new row" placeholder row, and a multi-select spanning it puts a non-model sentinel object into `SelectedItems`, which the code-behind's `foreach (Connection c in SelectedItems)` then tried to implicit-cast. Fixed (ADR-014) with `CanUserAddRows="False"` on all four grids (Devices/Connections/Cables/Cores — none of them use the native inline-add row anyway) plus a defensive `SelectedItems.OfType<T>()` in every selection-changed handler so this can't recur even if a future grid re-enables it. No test changes (App-layer-only, no UI test suite).
- **2026-07-09** — User clarified grid-delete semantics after finding Connection deletion worked but shouldn't: Connections are a pure derived fact of two symbols' geometry (M7's auto-connect), so they have no standalone identity to delete — removed "Delete Selected" from the Connections tab entirely, not just guarded it (ADR-015). Devices/Cables ARE parts with independent identity, so deleting one is legitimate, but should mean "delete this part everywhere it's drawn" — added `ProjectSession.DeleteDeviceCascade` (deletes a Device and every Placement/symbol of it across every page, plus dependent Connections, raising each live-sync event once for the whole batch) and switched the Devices grid's Delete Selected to always use it, with a confirmation that says symbols will be removed too. This let us delete `ProjectSession.CanDeleteDevice`/`DeleteDevice` outright — `DeleteDeviceCascade` supersedes every case they covered, and they had no other caller. 3 tests replaced (cascade across pages, dependent-connections-plus-single-event-per-batch, zero-Placement fallback) — 140 tests passing, unchanged net count.
- **2026-07-09** — User confirmed the cascade-delete fix works live. **M8 (Grid Editor) is now fully confirmed working end-to-end**, after five rounds of live-testing bug fixes (ADR-011 through ADR-015: graphics-first creation rollback, the Part-caching FK crash, the Delete Selected no-op, the DataGrid placeholder-row crash, and the Device/Cable/Connection delete-semantics distinction). Next: M9 (cable & termination model) or another direction — to be decided with the user.
- **2026-07-09** — User asked when general visual polish/styling (not functional UI) would happen — none of the windows built so far (M2–M8) have had any dedicated visual design, all default WPF. Logged into `REQUIREMENTS.md`'s Phase 3 as a deliberate deferral: doing it now would mean re-styling every time a new M9–M12 window/grid lands, so it belongs alongside the installer/onboarding work once the functional set stops changing shape. M9 (cable & termination model) chosen as next.
- **2026-07-09/10** — M9 (cable & termination model) chosen and completed. Confirmed with the user first that three REQUIREMENTS "auto-populate/derive" pieces (stripping length from part data, cable cores from a cable Part's template, `Cable.EndTypeClassification` derivation) have no real data source anywhere and stay manual-entry-only, focusing M9 on per-end termination toggle/type/part plus the filterable bulk-assign view. Built as a 4th Grid Editor tab, reusing every M8 pattern as-is (flattened read-shape row, `EnsurePartCached`, purpose-built toolbars, live-sync fan-out) — no new architectural decisions, no new ADR. See the M9 checklist above. 149 tests passing total (8 Core + 58 Rendering + 83 Data, up from 140); build and full test suite passed clean on the first real attempt. User confirmed the Terminations tab works live — **M9 fully confirmed, unlike M8 this landed with zero live bugs found.**
- **2026-07-10** — With M9 confirmed, user redirected priorities away from the remaining planned milestones (then M10/M11/M12 — Reports/Export/Hardening) toward the application shell itself: "it cannot be windows floating on top of each other... I'm able to open and edit symbol devices with double clicking while the page is open... when I open the program I get recent files, and automatically open the latest project... open the first page of the project." Planned as a new M10 (renumbering the existing three to M11/M12/M13) via `EnterPlanMode` with three parallel Explore agents researching `MainWindow`/startup state, `SchematicPageWindow`'s exact Window-coupled assumptions, and the existing window patterns (Grid Editor/Parts Library/Symbol Browser). See ADR-016 for the full design. Phase 1 (tabbed shell) built: `DocumentTabViewModel` + `MainViewModel.OpenTabs`/`SelectedTab`, `MainWindow` restructured to a Pages-sidebar + document-`TabControl` layout with implicit `DataTemplate`s, `SchematicPageWindow` extracted into `SchematicPageView` (`UserControl`) and its per-page window registry deleted, Ctrl+Click cross-page navigation rewired to find-or-create a page tab. Live-tested bug found and fixed immediately: a newly-opened tab didn't render until a stray click forced a repaint — root cause was the implicit-`DataTemplate` View getting its `DataContext` assigned after construction (so wiring `RedrawRequested` in the constructor, as the old Window did, never fired) compounded by `SKElement` not reliably self-painting the first time it's freshly templated into a tab; fixed with a `DataContextChanged` handler plus an explicit `InvalidateVisual()` there and on `Loaded`.
- **2026-07-10** — Continuing live feedback on the M10 shell, addressed directly without re-entering full plan mode each time (per this project's established "small follow-up requests don't need a fresh EnterPlanMode cycle" pattern): (1) grid rendering changed from lines to dots, with an adjustable-density combo box (`GridSpacingOptions` = 10/20/40/80) — confirmed via the existing architecture that `Placement.X/Y` is always stored in absolute world units, so changing grid density can never move anything already placed; (2) the app now auto-reopens the last project on launch (new `AppSettingsStore`, a small JSON file at `%LOCALAPPDATA%\Ecad\settings.json`, mirroring `LibraryDatabase`'s path idiom) unless it was explicitly closed last session, guarding with `File.Exists` first since `Microsoft.Data.Sqlite` would otherwise silently create an empty junk file at a stale path; (3) symbols can now be dragged from the palette straight onto the canvas, alongside the existing click-to-place flow. Deliberately did NOT build a Recent Files menu — only "auto-open the latest project" was actually asked for.
- **2026-07-10/11** — User asked for right-drag multi-select (replacing right-drag-to-pan) with panning moved to middle-click-drag instead. Confirmed via `AskUserQuestion` that multi-selected placements should support both group-delete and group-move (not delete-only). Built `PlacementHitTester.HitTestRect` (AABB intersection, deliberately not rotation-aware — accepted simplification for a marquee select), a `RubberBandRenderInfo` render pass in `SchematicCanvasRenderer` (dashed rectangle with translucent fill), and a new `CompositeCommand` (wraps several `IUndoableCommand`s so a multi-select group action undoes/redoes as one atomic step). `SchematicPageViewModel` gained `SelectedPlacementIds`, rubber-band drag state, and a unified single-vs-group drag path (`_dragGroupOriginalPositions`, Count==1 is just the single-item case of the same mechanism) — group drags snap the shared delta as one, preserving exact relative offsets between members (per-item magnet-pin-snap stays single-item-only, since snapping each member independently would let a group drift apart). Confirmed with the user: Rotate (`R`) and wire/connection selection stay single-select only, no additive Ctrl+click selection (each new rubber-band drag replaces the prior selection). 153 tests passing total (149 + 4 new `HitTestRect` tests); build and tests passed clean on the second attempt (a `CS0136` local-variable-name conflict from reusing `snappedX`/`finalX` across sibling scopes, fixed by renaming). User confirmed multi-select, group move, and group delete all work live, and confirmed panning via middle-click-drag.
- **2026-07-11** — Continued M10 with Phase 2 and Phase 3 (both from the original ADR-016 plan, done back-to-back while the user was away and unable to click-test live — build/test-verified only, live confirmation still pending): `GridEditorWindow` extracted into `GridEditorView`, opened as a singleton project-scoped tab found-or-created by ViewModel type (`OpenTabs.FirstOrDefault(t => t.Content is GridEditorViewModel)`), the same shape as the page tabs' `PageId`-keyed lookup but simpler since there's only ever one Grid Editor. `PartsLibraryWindow`/`SymbolBrowserWindow` extracted into `PartsLibraryView`/`SymbolBrowserView` the same way, but as `IsProjectScoped = false` tabs (survive Close Project, matching the old windows never holding a `ProjectSession`) — their `RelativeSource AncestorType=Window` bindings (for the Parts Library's image/sub-grid columns) became `AncestorType=UserControl`, same fix Phase 1 already needed for `SchematicPageView`'s palette button. Neither view needed Phase 1's `DataContextChanged`/`InvalidateVisual` workaround — both are ordinary WPF-bound controls (`DataGrid`/`ListView`/`ItemsControl`), not a manually-painted `SKElement`. All four old `*Window` files deleted. 153 tests passing (unchanged — App-layer-only, no new automated tests possible for this kind of change, per this project's established pattern). Remaining M10 work: Phase 4 (docked device-properties panel, replacing the modal rename dialog) — not started.
- **2026-07-11** — User asked to swap the multi-select/pan mapping again: left-click should do the selecting (not right-click), with right-click free to "display options." Confirmed via `AskUserQuestion` that this meant building a real context menu now, not just freeing up the button. Left-click-drag on empty space now performs the rubber-band multi-select — but only once the drag crosses a small pixel threshold (`_isRubberBandArmed`/`_isRubberBandSelecting` handshake across `HandleLeftButtonDown`/`HandleMouseMove`/`HandleLeftButtonUp`), so a stationary click still selects a wire or deselects exactly like before (a rubber-band hit-test only checks placements, so treating every click as an instant marquee would have silently broken "click a wire to select it"). Right-click (`HandleRightClick`) selects whatever's under the cursor without starting a drag, then WPF's own `ContextMenu` (declared on the `SKElement`, `PlacementTarget.DataContext`-bound since a `ContextMenu` is a separate popup that doesn't inherit `DataContext`) opens with Rotate/Delete/Undo/Redo. `HandleKeyDown`'s inline Delete/R logic was extracted into `DeleteSelectionCommand`/`RotateSelectionCommand` so the keyboard shortcuts and the context menu share one implementation and one enabled/disabled state.
- **2026-07-11** — Follow-up: user wants the rubber-band to behave like EPLAN/AutoCAD — a left-to-right drag should require a placement to be fully inside the box, while a right-to-left drag should only need to touch it. Added an optional `requireFullContainment` flag to `PlacementHitTester.HitTestRect` (2 new tests: full-containment excludes a partial overlap, includes full containment); `FinishRubberBandSelection` picks the mode from drag direction (`_rubberBandCurrentScreen.X >= _rubberBandStartScreen.X` = window/left-to-right, else crossing/right-to-left). 155 tests passing (153 + 2). Immediately after, the user separately reported that dragging (rubber-band or an object) out past the canvas's edge made the drag freeze until the cursor wandered back in — a real WPF gotcha: without explicit mouse capture, a control stops receiving `MouseMove`/`MouseUp` the instant the cursor leaves its bounds. Fixed with `SkiaCanvas.CaptureMouse()` on left/middle button-down and `ReleaseMouseCapture()` on the matching button-up, plus a defensive `CancelActiveDrag()` (reverts any live-dragged placement back to its pre-drag position) wired to `LostMouseCapture`, in case capture is ever taken away by something else (e.g. focus stolen by another window) before a button-up arrives. User confirmed both the direction-aware selection and the capture fix work live.
- **2026-07-11** — M10 Phase 4 (docked device-properties panel) built, completing the whole M10 milestone. `SchematicPageViewModel` gained the docked-panel state directly (no new VM class — matches the original plan's "moderate state, not enough to justify a new composition layer" call): `IsDevicePanelOpen`, `DevicePanelFunction`/`Location`/`Designation` with a live `DeviceTag`-preview computed on every keystroke, `DevicePanelValidationText`, `ApplyDeviceEditCommand`/`CloseDevicePanelCommand`. `HandleDoubleClick`'s existing "construct `DeviceTagDialog`, `ShowDialog()`, read the result back" became "populate this state, set `IsDevicePanelOpen = true`" for the rename-an-existing-placement path only — new-placement's tag prompt and wire-rename both keep their original modal dialogs untouched, exactly as scoped in the original M10 plan. `SchematicPageView` gained a collapsible `Border` docked to the right of the canvas (`Visibility` bound through a `BooleanToVisibilityConverter`), visible only while the panel is open. `DeviceTagDialog`'s now-unused rename-mode constructor was deleted (not left as dead code), simplifying the dialog down to its one remaining new-placement mode. 155 tests passing (unchanged — App-layer-only). **M10 is now fully complete** (Phases 1-4, plus the grid-dots/auto-reopen/drag-and-drop/multi-select/context-menu/mouse-capture follow-ups along the way) — pending the user's live click-through of Phase 4 specifically (Phases 1-3 and the selection/pointer model were already confirmed working live in earlier rounds).
- **2026-07-11** — With M10 confirmed, user redirected again: before Reports (M11 at the time), the app needed a better way to number connections, replacing M7's automatic-on-creation wire numbering with an EPLAN-style explicit placement gesture — "I want the lines to have nothing, till I put a connection definition point... a diagonal line on the line, and can store... wire cross section, color, and number." Cables (a line crossing multiple conductors) were named as the deliberate next step after that. Planned via `EnterPlanMode` (3 parallel Explore agents, 4 `AskUserQuestion` clarifications on marker-removal semantics/auto-suggest/label content/placement gesture). Built: `Connection.DefinitionPointPositionT` (a 0..1 route-relative fraction, since a wire's route is recomputed live from pin positions — ADR-009), a "Place Definition Point" toolbar toggle, `DefinitionPointDialog` (Wire#/Color/Cross-section + Remove), and `SetDefinitionPointCommand`/`MoveDefinitionPointCommand` covering place/edit/remove/drag-reposition/drag-relocate-to-a-different-wire. Immediately followed by a selection-model fix after the user found a real bug: double-clicking a bare wire could still select the underlying `Connection` (via the click portion of the double-click) and let Delete remove the whole wire — extended ADR-015's "a connection has no independent identity" principle from the Grid Editor to the canvas, removed wire/connection selectability entirely, and extended rubber-band selection to cover definition-point ticks. 169 tests passing at this point (up from 155).
- **2026-07-12** — User reported the actual motivating bug directly: moving a connected symbol made its definition point disappear. Root cause, found by tracing `SchematicPageViewModel.RunAutoConnect`: it deletes and recreates the `Connection` row for any pin pair that stops facing each other after a move (nearly every free drag), and the definition point's data lived on that now-deleted row — a genuine design flaw, not a patchable bug. Redesigned as a fully independent `DefinitionPoint` entity mirroring `Placement`'s own pattern (own table, own `PageId`, absolute X/Y, optional `ConnectionId` with `ON DELETE SET NULL` so an unrelated auto-connect rewrite just detaches it instead of destroying it), with its fields mirrored one-way onto the attached `Connection`'s own columns (so Grid Editor/Terminations keep working unchanged) — confirmed via `AskUserQuestion` that placement should work in free space too (not just snapped onto a wire) and that Grid Editor's Color/Cross-section should become read-only once a definition point is attached (matching Wire#'s existing treatment). Migrations 0004→0005 evolved accordingly (new table, backfill, old column dropped); `WireNumberDialog`/`RenameWireNumberCommand` deleted outright, superseded. See ADR-018. Immediately after, two more live-testing fixes: dragging a definition point wasn't snapping to the grid at all (fixed), and — per direct request — the tick gained a rotatable `RotationDegrees` (R key, 90° per press, same convention as symbols) and switched from black to red as its default color (DodgerBlue still highlights when selected). 191 tests passing at this point.
- **2026-07-12** — Cable definition lines chosen next, per the user's own stated ordering from the message above. REQUIREMENTS 6.1's one-line spec ("draw across connections to assign them to a cable; core assignment dialog") and ADR-011's explicit deferral note were the starting point; `AskUserQuestion` clarified two design points before planning: a straight-segment gesture (not a multi-point polyline) and — after the user's own follow-up clarification — that no upfront cable/core setup should be required at all: draw the line, type a cable name, and every crossed wire is auto-assigned a core automatically, with no separate per-wire picker dialog. Built `CableLine`/`CableLineCrossing` tables (the same independent-entity + `ON DELETE SET NULL` shape `DefinitionPoint` had just been redesigned into, so a crossed wire's connection being rewired elsewhere orphans just that one crossing rather than destroying the line) and a new `SegmentIntersection` primitive (segment/polyline crossing math — confirmed nothing like it existed anywhere in `Ecad.Rendering`). `ProjectSession.DrawCableLine`/`MoveCableLine`/`ReassignCableLine`/`DeleteCableLine` reuse the existing `UpdateConnectionCable` write path from M8, auto-creating a sequentially-numbered `CableCore` per newly-detected crossing and skipping (never overwriting) a wire already assigned to a different cable. This is the actual resolution of ADR-011's "revisit once cable-definition-line drawing is built" note — the Grid Editor's Cables tab is unchanged and stays how a core's Color/CrossSectionMm2/type gets filled in after the fact, and the only way to create a Cable with zero wires. See ADR-019. 196 tests passing.
- **2026-07-12** — Four rounds of live feedback on the cable line refined it further, all same-day: (1) drag either endpoint independently to extend/shrink an existing line, not just translate the whole thing; (2) the circular crossing marker became the same red diagonal-tick glyph a wire definition point uses, for visual consistency; (3) each crossing became independently selectable (click/right-click/rubber-band), rotatable (R key), and double-click-editable via a new `CableCoreDialog` (core number/color/cross-section, with a uniqueness check against the cable's other cores) — extending the exact same selection/rotation/property-edit shape just built for wire definition points onto cable crossings, per direct request; (4) a grid-snap bug in whole-line dragging (computing its offset from the raw unsnapped cursor rather than a snapped anchor position — the same bug class already fixed once for definition points) was reported and fixed. Test count unchanged from the initial cable-line landing (196) — these were all interaction/rendering refinements to already-tested data-layer methods, verified live. Next: M12 (Reports engine) or another direction — to be decided with the user.
- **2026-07-09/12** — Between the cable-line work and M12, git usage was set up for the whole project: the local repo (initialized back at M0, never pushed anywhere) was reconciled into four clean commits (M8/M9/M10/M11, bucketed by which milestone last substantively touched each file, since none of that work had been committed incrementally) and pushed via a new HTTPS remote to the user's own personal GitHub account (`github.com/allamhanna/ECAD`) — explicitly *not* the user's company GitLab, confirmed directly after a tool-call rejection flagged a concern about exactly that. No functional code changes; recorded here since it changes how future milestones should be committed (one commit per milestone going forward, previously zero).
- **2026-07-12** — M12 (Reports engine) chosen next per the user's own prior ordering. Researched REQUIREMENTS.md Section 5.9 (layout schema) and 6.4 (the four Phase-1 reports) plus the existing `Ecad.Reports` stub (QuestPDF already referenced since M0, otherwise empty) and data model via parallel Explore agents, then a Plan agent to work out the concrete architecture, verifying its output line-by-line against the actual code rather than trusting it blindly — this caught three stale assumptions (`GetAllConnections`/`GetAllConnectionEndsWithContext` already existed project-wide; `PageType.Report` already existed in the enum, unused; `Page` had no update/delete path anywhere, a genuine gap M12 had to fill first). Built the plan as designed, including QuestPDF-based rendering (verified its fluent API against the package's own shipped XML docs rather than guessing, per this project's habit since being burned in M3/M4 — it built and passed all 15 new tests on the first real attempt).
- **2026-07-12** — Mid-build, the user stopped the QuestPDF direction: reports should be a plain data page auto-generated from the drawings, not a PDF, and QuestPDF's commercial-license cost isn't something to accept silently for the core report-viewing path — PDF/Excel/CSV export stays deliberately deferred to M13, technology TBD then. Removed QuestPDF from `Ecad.Reports` entirely the same day (package reference + the whole rendering folder); the JSON layout schema and all four report Builders needed zero changes (always pure data transforms, no rendering dependency) — only `ReportEngine` (shrank to template loading/lookup) and `ReportPageViewModel`/`ReportPageView` (now expose/show plain header fields + a `DataGrid` instead of rasterized preview images) changed. See ADR-020's revision note and the M12 checklist above for the corrected build. 220 tests passing total (8 Core + 86 Rendering + 111 Data + 15 Reports, up from 196) — unchanged by the pivot, since the Reports test suite never touched the removed rendering layer; the app was rebuilt and relaunched successfully both before and after. The actual Reports menu/DataGrid click-through is still pending the user, same "I can't drive a rendered WPF window myself" limitation as every other canvas/window milestone. Next: M13 (Export: PDF/Excel/CSV, technology TBD) or the user's live-testing feedback on M12 — to be decided with the user.
